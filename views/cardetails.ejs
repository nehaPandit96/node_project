<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Car Details</title>
    <style>
      /* Navbar container */
      .navbar {
        overflow: hidden;
        background-color: #333; /* Dark background color */
      }

      /* Links inside the navbar */
      .navbar a {
        float: left;
        display: block;
        color: #f2f2f2; /* White text color */
        text-align: center;
        padding: 14px 20px; /* Padding for each link */
        text-decoration: none;
      }

      /* Change background color of links on hover */
      .navbar a:hover {
        background-color: #ddd; /* Light grey background color */
        color: black; /* Black text color */
      }
    </style>
  </head>
  <body>
    <div class="navbar">
      <a href="/">Home</a>
      <a href="/add">Add a New car</a>

      <% if (user) { %>
      <a href="/logout">Logout</a>
      <% } else { %>
      <a href="/login">Login</a>
      <a href="/register">Register</a>
      <% } %>
    </div>
    <h1>Car Details</h1>
    <ul>
      <li>Name: <%= car.manufacturer %></li>
      <li>Price: <%= car.price %></li>
      <li>Model: <%= car.model %></li>
      <li>Year: <%= car.year %></li>
      <li>Color: <%= car.color %></li>

      <li>VIN (Vehicle Identification Number): <%= car.vin %></li>

      <li>Engine Type: <%= car.engineType %></li>

      <li>Mileage: <%= car.mileage %></li>
      <li>Fuel Type: <%= car.fuelType %></li>
      <li>Transmission Type: <%= car.transmissionType %></li>

      <li>Status: <%= car.status %></li>
      <li>Images:</li>
      <div>
        <% car.images.forEach(image => { %>
        <div>
          <img
            src="<%= image %>"
            alt="<%= car.name %>"
            height="300"
            width="300"
          />
        </div>
        <% }); %>
      </div>
    </ul>
    <% if (user && user.role !== 'user') { %>
    <a href="/update/<%= car._id %>">Update</a>
    <% } %>
    <!-- mark sale as pending -->
    <% if (user && user.role !== 'user') { %>
    <form action="/markPendingSale/<%= car._id %>" method="POST">
      <button type="submit" class="deletebtn">Mark as pending sale</button>
    </form>
    <% } %>
  </body>
</html>
